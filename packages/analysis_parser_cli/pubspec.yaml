name: analysis_parser_cli
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  args: '^2.4.2'
  file: ^7.0.0
  collection:
  process:
    git:
      url: https://github.com/kannel-outis/process.git

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
